#!/usr/bin/env bash
echo "Installing Ghostscript"
GS_VERSION=9.19
GS_TGZ=ghostscript-$GS_VERSION.tar.gz
INSTALL_DIR=$BUILD_DIR/vendor/ghostscript/
mkdir -p $INSTALL_DIR
tar -zxvf $ROOT_DIR/$GS_TGZ -C $INSTALL_DIR
pwd
source $INSTALL_DIR/ghostscript-$GS_VERSION/configure --prefix=$INSTALL_DIR
make
make install
ls $INSTALL_DIR
echo "Ghostscript installed"