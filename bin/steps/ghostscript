#!/bin/bash
GS_VERSION=9.18

GS_TGZ=ghostscript_$GS_VERSION.tar.xz

INSTALL_DIR=$BUILD_DIR/vendor/ghostscript/
GS_DIR=$BUILD_DIR/vendor/ghostscript

echo "Unpacking ghostscript binaries"
mkdir -p $INSTALL_DIR
tar -zxvf $ROOT_DIR/$GS_TGZ -C $INSTALL_DIR

